# Dynamic 3DGS Compression Benchmark: results page

Served at https://wcjj1236.github.io/d3dgs-benchmark/

This repository only hosts the built page. Its source, the data it is built from and the
build script live in `site/` of the benchmark repository
(https://github.com/wcjj1236/dynamic-3dgs-compression-benchmark); edit there and publish
with `site/deploy.sh`, which overwrites this repository's files.
